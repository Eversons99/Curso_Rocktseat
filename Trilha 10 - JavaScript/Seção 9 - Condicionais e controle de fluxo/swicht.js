let expresion = 'A'

/* o switch recebe uma expressão e compara com as condições do CASE, caso a expressão se encaixe em um dos valores ele exucuta o codigo, 
caso não se encaixe em nenhum case a expressão é jogada para o default, é importante lembrar de colocar o break para poder delimitar o fim de cada case*/
switch (expresion) {
    case 'a':
        //codigo
        break

    case 'a':
        //codigo
        break

    default:

        break
}