// Operador condicional Ternário

/* Dependendo da condição, nós recebemos valores diferentes */

// condition ? value1 (se) : value2 (senão) 

// Exemplos
let pao = false
let queijo = false

const cafeDaManha = pao || queijo ? true : false

console.log(cafeDaManha)